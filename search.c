#include <stdio.h>

/*
 * (key - low)/(a[key] - a[low]) = (high - low)/(a[high] - a[low])
 * key = low + (high - low)/(a[high] - a[low]) * a[key] - a[low]
 */

int interpolation_search(int *array, size_t len)
{
	int index = 0;
	int mid =  0;

	return index;
}

int main(int argc, char** argv)
{
	int ret = 0;

	return ret;
}

