#include <stdio.h>

/*
 * Given an N*N matrix of positive and negative integers
 * find the sub-matrix with the largest possible sum
 * print the sum
 */

int max_sum_for_submatrix(unsigned int size, int** matrix)
{
	int ret = 0;

	return ret;
}

int main(int argc, char** argv)
{
	
	return 0;
}
